name: Testing a name with input ${{ inputs.name }}

on:
  workflow_dispatch:
    inputs:
      name:
        description: "Just the name"
        required: true

jobs:
  echo:
    runs-on: ubuntu-latest
    steps:
      - name: echo
        run: echo "Hello, from ${{ inputs.name }}!"
